'use strict';

/**
 * @ngdoc function
 * @name healthTrackerApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the healthTrackerApp
 */
angular.module('healthTrackerApp')
  .controller('UserCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
